import { registerRootComponent } from 'expo';
import 'react-native-gesture-handler'; // Import to initialize gesture handler

import App from './App';

// Register the root component (App) which will render the application
registerRootComponent(App);
